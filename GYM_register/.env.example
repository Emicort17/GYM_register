# Copia este archivo a ".env" (en la misma carpeta que docker-compose.yml) y
# ajusta los valores. docker compose lo carga automáticamente si existe.
# El archivo ".env" real no se sube a git (ver .gitignore).

DB_NAME=gestion_persona
DB_PASSWORD=changeme
SERVER_PORT=8000
